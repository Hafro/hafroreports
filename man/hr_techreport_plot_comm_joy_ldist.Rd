% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hr_techreport_plot_comm_joy_ldist.R
\name{hr_techreport_plot_comm_joy_ldist}
\alias{hr_techreport_plot_comm_joy_ldist}
\title{Plot commercial length-frequency distribution as a joy plot}
\usage{
hr_techreport_plot_comm_joy_ldist(
  pcon,
  length_min = 0,
  length_max = 1e+06,
  year_start = 1000,
  year_end = 9999,
  mfdb_gear_codes = c("BMT", "DSE", "LLN", "GIL"),
  sampling_types = c(1, 2, 4, 8),
  max_height = 50,
  split_by_sex = FALSE
)
}
\arguments{
\item{pcon}{A database connection object compatible with \code{dplyr::tbl}.}

\item{length_min}{Numeric. Minimum length to include (exclusive).
Default is \code{0}.}

\item{length_max}{Numeric. Maximum length to include (exclusive).
Default is \code{1e6}.}

\item{year_start}{Integer. First year to include. Default is \code{1000}.}

\item{year_end}{Integer. Last year to include. Default is \code{9999}.}

\item{mfdb_gear_codes}{Character vector of MFDB gear codes to include.
Default is \code{c("BMT", "DSE", "LLN", "GIL")}.}

\item{sampling_types}{Integer vector of sampling type codes. Default is
\code{c(1, 2, 4, 8)}.}

\item{max_height}{Numeric. Scaling factor for ridge height.
Default is \code{50}.}

\item{split_by_sex}{Logical. If \code{TRUE}, produces separate ridges
for each sex. Default is \code{FALSE}.}
}
\value{
A \code{ggplot2} plot object.
}
\description{
Queries the \code{station} table of a pax database, filters by gear code
and sampling type, and produces a ridgeline (joy) plot of length
distributions by year.
}
