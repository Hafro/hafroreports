% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_data.R
\name{hr_input_data_maturity_key}
\alias{hr_input_data_maturity_key}
\title{Estimate maturity-at-length key from survey data}
\usage{
hr_input_data_maturity_key(
  pcon,
  lgroups = seq(0, 200, 5),
  regions = NULL,
  ignore_years = c(),
  sampling_type = 30
)
}
\arguments{
\item{pcon}{A database connection object compatible with \code{dplyr::tbl}.}

\item{lgroups}{Numeric vector of length group break points (lower bounds).
Default is \code{seq(0, 200, 5)}.}

\item{regions}{Named list mapping region labels to integer MFDB area
codes. If \code{NULL}, all stations are treated as one region
(\code{"all"}). Default is \code{NULL}.}

\item{ignore_years}{Integer vector of years to exclude from model fitting.
Default is \code{c()} (no years excluded).}

\item{sampling_type}{Integer vector of sampling type codes to include.
Default is \code{30}.}
}
\value{
A tibble with columns \code{year}, \code{lgroup}, \code{age},
\code{region}, and \code{mat_p}. Rows with \code{year = NA} are
model predictions.
}
\description{
Builds a maturity ogive by fitting a quasi-binomial GLM to maturity
observations from the \code{measurement} table, grouped by length class
and region. Observed proportions mature (by year, length group, age,
and region) are combined with model-predicted proportions (with
\code{year = NA} and \code{age = NA}) in the output, so downstream
code can distinguish measurements from estimates.
}
