% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/muppet_input.R
\name{hr_muppet_input_optionfile}
\alias{hr_muppet_input_optionfile}
\title{Prepare a MUPPET option file for a given assessment year}
\usage{
hr_muppet_input_optionfile(
  opt_file,
  out_name,
  year_end,
  age_end = 10,
  plus_group = 1
)
}
\arguments{
\item{opt_file}{Character string containing the full text of the MUPPET
option file template. Lines are delimited by \code{"\n"}.}

\item{out_name}{Character. Name to use as the key in the returned list,
typically the relative path to the option file within the MUPPET run
directory (e.g. \code{"params/had.dat.opt"}).}

\item{year_end}{Integer. The last assessment year. Used to set the last
data year, last optimisation year, last SMH year, and last SMB year.}

\item{age_end}{Integer. The last model age. Default is \code{10}.}

\item{plus_group}{Integer flag (0 or 1) indicating whether the last age is
a plus group. Default is \code{1}.}
}
\value{
A named list with one element: \code{out_name} mapped to the
updated option file text.
}
\description{
Takes a template MUPPET option file (as a single character string) and
updates the key year and age settings using \code{rmuppet::line_replace}.
Returns a named list suitable for passing to
\code{\link{hr_muppet_run}} as part of the input file set.
}
