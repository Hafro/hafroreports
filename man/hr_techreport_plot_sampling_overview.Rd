% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hr_techreport_plot_sampling_overview.R
\name{hr_techreport_plot_sampling_overview}
\alias{hr_techreport_plot_sampling_overview}
\title{Plot sampling coverage by month, gear, and sampling type}
\usage{
hr_techreport_plot_sampling_overview(
  pcon,
  mfdb_gear_codes = c("LLN", "DSE", "BMT"),
  sampling_types = c(1, 2, 3, 4, 8),
  gear_group = list(BMT = "BMT", LLN = "LLN", DSE = "DSE"),
  year_start = 1000,
  year_end = 9999
)
}
\arguments{
\item{pcon}{A database connection object compatible with \code{dplyr::tbl}.}

\item{mfdb_gear_codes}{Character vector of MFDB gear codes to include.
Default is \code{c("LLN", "DSE", "BMT")}.}

\item{sampling_types}{Integer vector of sampling type codes. Default is
\code{c(1, 2, 3, 4, 8)}.}

\item{gear_group}{Named list mapping gear group labels to MFDB gear codes,
used to group stations and landings by gear. Default groups are
\code{BMT}, \code{LLN}, and \code{DSE}.}

\item{year_start}{Integer. First year to include. Default is \code{1000}.}

\item{year_end}{Integer. Last year to include. Default is \code{9999}.}
}
\value{
A \code{ggplot2} plot object.
}
\description{
Creates a faceted tile/bar plot comparing the monthly distribution of
biological samples to the monthly distribution of landings, grouped by
gear and year. Sampling type is shown as stacked bar fill; the number of
samples per month is annotated above each bar.
}
